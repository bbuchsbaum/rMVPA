# Select lambda by prototype self-retrieval on training items

Select lambda by prototype self-retrieval on training items

## Usage

``` r
.select_lambda(Xw_fit, Yw_fit, Delta, lambda_grid)
```

## Value

A numeric scalar: the selected lambda value.
