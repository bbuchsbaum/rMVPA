# Residual reduced-rank regression: (Yw - Xw) ~ Xw

Residual reduced-rank regression: (Yw - Xw) ~ Xw

## Usage

``` r
.fit_residual_rrr(Xw, Yw, rank = "auto", max_rank = 20, ridge_lambda = NULL)
```

## Value

A list with fitted residual RRR components.
