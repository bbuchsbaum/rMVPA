# Internal helper: reduced-rank regression fit for repmap_model

Internal helper: reduced-rank regression fit for repmap_model

## Usage

``` r
.repmap_fit_rrr(X, Y, rank = "auto", max_rank = 20, ridge_lambda = NULL)
```

## Value

A list with fitted repmap RRR components.
