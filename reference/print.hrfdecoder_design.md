# Print method for hrfdecoder_design

Print method for hrfdecoder_design

## Usage

``` r
# S3 method for class 'hrfdecoder_design'
print(x, ...)
```

## Arguments

- x:

  An hrfdecoder_design object

- ...:

  Additional arguments (ignored)

## Value

Invisibly returns the input object `x` (called for side effects).

## Examples

``` r
if (FALSE) { # \dontrun{
  # Requires hrfdecoder and fmridesign packages
  # print(hrfdecoder_design_object)
} # }
```
