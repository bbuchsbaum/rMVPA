% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remap_utils.R
\name{summarize_remap_items}
\alias{summarize_remap_items}
\title{Summarize REMAP item-level residuals for an ROI}
\usage{
summarize_remap_items(x, roi = NULL)
}
\arguments{
\item{x}{Either a `regional_mvpa_result` (preferred) or a predictor list
containing `diag_by_fold`.}

\item{roi}{ROI index or id when `x` is a regional result. If `NULL` and `x`
is a predictor, it is ignored.}
}
\value{
A tibble with columns: `item`, `res_naive`, `res_remap`, `res_ratio`,
  and `n_folds`.
}
\description{
Extract per-item distances between memory and perception prototypes in the
jointly-whitened space, before (naive) and after the REMAP correction. Works
from a `regional_mvpa_result` when `return_fits=TRUE`, or directly from a
predictor list returned by `process_roi()`.
}
\examples{
\dontrun{
res <- run_regional(model, region_mask, return_fits = TRUE)
items_tbl <- summarize_remap_items(res, roi = 1)
}
}
