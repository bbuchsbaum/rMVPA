% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allgeneric.R, R/crossval.R, R/mvpa_model.R
\name{crossval_samples}
\alias{crossval_samples}
\alias{crossval_samples.sequential_blocked_cross_validation}
\alias{crossval_samples.kfold_cross_validation}
\alias{crossval_samples.blocked_cross_validation}
\alias{crossval_samples.bootstrap_blocked_cross_validation}
\alias{crossval_samples.custom_cross_validation}
\alias{crossval_samples.twofold_blocked_cross_validation}
\alias{crossval_samples.mvpa_model}
\title{Cross-validation samples}
\usage{
crossval_samples(obj, data, y, ...)

\method{crossval_samples}{sequential_blocked_cross_validation}(obj, data, y, ...)

\method{crossval_samples}{kfold_cross_validation}(obj, data, y, ...)

\method{crossval_samples}{blocked_cross_validation}(obj, data, y, ...)

\method{crossval_samples}{bootstrap_blocked_cross_validation}(obj, data, y, ...)

\method{crossval_samples}{custom_cross_validation}(obj, data, y, id = ".id", ...)

\method{crossval_samples}{twofold_blocked_cross_validation}(obj, data, y, ...)

\method{crossval_samples}{mvpa_model}(obj, data, y, ...)
}
\arguments{
\item{obj}{A cross-validation control object.}

\item{data}{A data frame containing the predictors.}

\item{y}{A vector containing the response variable.}

\item{...}{Extra arguments passed to the specific cross-validation methods (e.g., `id` for custom cross-validation).}

\item{id}{Column name used for the fold identifier column in the returned tibble.}
}
\value{
A tibble containing training and testing sets for each fold.
}
\description{
Apply a cross-validation scheme to split the data into training and testing sets.
}
\examples{
cval <- kfold_cross_validation(len = 20, nfolds = 4)
dat  <- as.data.frame(matrix(rnorm(20 * 2), 20, 2))
y    <- factor(rep(letters[1:4], 5))
crossval_samples(cval, dat, y)
}
