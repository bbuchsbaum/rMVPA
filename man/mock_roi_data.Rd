% Generated by hand; edit R/plugin_helpers.R and regenerate if needed
\name{mock_roi_data}
\alias{mock_roi_data}
\title{Build Mock ROI Data for Plugin Tests}
\usage{
mock_roi_data(
  train_data = NULL,
  test_data = NULL,
  indices = NULL,
  n_train = 20L,
  n_features = 10L,
  n_test = 0L,
  seed = NULL,
  train_roi = NULL,
  test_roi = NULL
)
}
\arguments{
\item{train_data}{Optional matrix of training data (observations x features).}

\item{test_data}{Optional matrix of test data (observations x features).}

\item{indices}{Optional integer feature indices. Defaults to \code{seq_len(ncol(train_data))}.}

\item{n_train}{Number of training observations to simulate when \code{train_data} is \code{NULL}.}

\item{n_features}{Number of features to simulate when \code{train_data} is \code{NULL}.}

\item{n_test}{Number of test observations to simulate when \code{test_data} is \code{NULL} and \code{n_test > 0}.}

\item{seed}{Optional RNG seed used only when simulating data.}

\item{train_roi}{Optional ROI object to include in the returned list.}

\item{test_roi}{Optional ROI object to include in the returned list.}
}
\value{
A list with fields \code{train_data}, \code{test_data}, \code{indices},
\code{train_roi}, and \code{test_roi}.
}
\description{
Constructs a minimal ROI payload compatible with \code{\link{fit_roi}}.
This is useful for unit-testing plugin methods without running
\code{run_searchlight()} or \code{run_regional()}.
}
