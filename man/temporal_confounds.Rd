% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal_rdms.R
\name{temporal_confounds}
\alias{temporal_confounds}
\title{Build multiple temporal confounds from a spec}
\usage{
temporal_confounds(
  spec,
  onsets,
  run = NULL,
  units = c("auto", "sec", "TR", "index"),
  TR = NULL,
  as_dist = TRUE
)
}
\arguments{
\item{spec}{a named list. Each element is a list describing one confound.
For kernel-based items, include at least \code{kernel}. For HRF items, set
\code{kind="hrf"} and provide \code{TR}. Common fields: \code{within_blocks_only}, \code{normalize}, \code{metric}.}

\item{onsets}{numeric vector of event onsets (seconds or TRs depending on \code{units})}

\item{run}{optional vector of run/block identifiers}

\item{units}{one of \code{"auto"}, \code{"sec"}, \code{"TR"}, or \code{"index"}}

\item{TR}{repetition time in seconds (optional for kernel, required for HRF)}

\item{as_dist}{logical; if TRUE return \code{dist} objects (default TRUE)}
}
\value{
A named list of \code{dist} objects (or matrices if \code{as_dist=FALSE})
}
\description{
Convenience function to produce a named list of temporal nuisance RDMs from
a compact specification. Items can be kernel-based (\code{temporal_rdm}) or
HRF-overlap based (\code{temporal_hrf_overlap}).
}
