% Generated by hand; edit R/plugin_helpers.R and regenerate if needed
\name{cv_evaluate_roi}
\alias{cv_evaluate_roi}
\title{Evaluate One ROI with rMVPA Cross-Validation Helpers}
\usage{
cv_evaluate_roi(
  model_spec,
  roi_data,
  context = NULL,
  mode = c("auto", "internal", "external"),
  return = c("roi_result", "row"),
  ...
)
}
\arguments{
\item{model_spec}{A model specification object.}

\item{roi_data}{ROI payload as used by \code{\link{fit_roi}}.}

\item{context}{Context list (defaults to \code{\link{mock_context}()} based on
\code{model_spec}).}

\item{mode}{One of \code{"auto"}, \code{"internal"}, or \code{"external"}.
\code{"auto"} chooses \code{"external"} when \code{has_test_set(model_spec)}
is true, otherwise \code{"internal"}.}

\item{return}{One of \code{"roi_result"} (default) or \code{"row"}.
\code{"row"} returns the raw tibble row produced by the CV helper.}

\item{...}{Additional arguments passed to the underlying CV helper.}
}
\value{
A \code{\link{roi_result}} by default, or a tibble row when
\code{return = "row"}.
}
\description{
Public wrapper around rMVPA's internal ROI cross-validation helpers.
This allows plugin authors to reuse standard fold execution without
calling non-exported functions.
}
