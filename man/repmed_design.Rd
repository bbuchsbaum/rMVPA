% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rena_design.R
\name{repmed_design}
\alias{repmed_design}
\title{Representational mediation design helper (ReNA-RM)}
\usage{
repmed_design(items, X_rdm, Y_rdm, confound_rdms = NULL)
}
\arguments{
\item{items}{Character vector of item IDs (keys).}

\item{X_rdm}{Predictor RDM (matrix or \code{"dist"}).}

\item{Y_rdm}{Outcome RDM (matrix or \code{"dist"}).}

\item{confound_rdms}{Optional named list of confound RDMs (matrix or \code{"dist"}).}
}
\value{
A list with elements:
  \itemize{
    \item \code{items}: character vector of items
    \item \code{X_rdm}, \code{Y_rdm}: predictor and outcome RDMs as matrices
    \item \code{confound_rdms}: named list of confound RDM matrices
  }
}
\description{
Construct a design object for representational mediation, with predictor (X)
and outcome (Y) RDMs over items and optional confound RDMs.
}
\examples{
\dontrun{
  items <- letters[1:10]
  X_rdm <- as.matrix(dist(matrix(rnorm(10*3), 10, 3)))
  Y_rdm <- as.matrix(dist(matrix(rnorm(10*3), 10, 3)))
  des <- repmed_design(items, X_rdm, Y_rdm)
}
}
