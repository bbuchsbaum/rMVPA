% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustered_dataset.R
\name{mvpa_clustered_dataset}
\alias{mvpa_clustered_dataset}
\title{Create an MVPA Dataset for Clustered/Parcellated Data}
\usage{
mvpa_clustered_dataset(train_data, test_data = NULL, mask = NULL)
}
\arguments{
\item{train_data}{A \code{ClusteredNeuroVec} instance (training data).}

\item{test_data}{An optional \code{ClusteredNeuroVec} instance (test data). Default: NULL.}

\item{mask}{An optional mask. If NULL, derived from \code{train_data}'s cluster volume.}
}
\value{
An \code{mvpa_clustered_dataset} object (S3 class) containing:
  \describe{
    \item{train_data}{The training data as a \code{ClusteredNeuroVec}.}
    \item{test_data}{The test data (if provided).}
    \item{mask}{The mask.}
    \item{cvol}{The \code{ClusteredNeuroVol} defining cluster assignments.}
    \item{region_mask}{A \code{NeuroVol} where each voxel's value is its cluster ID
      (for broadcasting results via \code{map_values}).}
    \item{has_test_set}{Logical flag.}
  }
}
\description{
Creates a dataset object for MVPA analysis on parcellated brain data stored
as \code{ClusteredNeuroVec} objects from \pkg{neuroim2}. A "clustered searchlight"
iterates over all K parcels, pooling nearby parcels as features for each analysis.
}
