% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/searchlight.R
\name{run_searchlight_base}
\alias{run_searchlight_base}
\title{A "base" function for searchlight analysis}
\usage{
run_searchlight_base(
  model_spec,
  radius = 8,
  method = c("standard", "randomized", "resampled"),
  niter = 4,
  combiner = "average",
  drop_probs = FALSE,
  fail_fast = FALSE,
  k = NULL,
  backend = c("default", "shard", "auto"),
  ...
)
}
\arguments{
\item{model_spec}{A model specification object (e.g., \code{mvpa_model}, \code{vector_rsa_model}, etc.).}

\item{radius}{Numeric searchlight radius (1 to 100).}

\item{method}{Character: "standard" or "randomized".}

\item{niter}{Number of iterations if \code{method="randomized"}.}

\item{combiner}{Either a function that combines partial results or a string
("pool", "average") that selects a built-in combiner.}

\item{drop_probs}{Logical; if TRUE, drop per-ROI probability matrices after computing metrics to save memory. Default FALSE.}

\item{fail_fast}{Logical; if TRUE, stop immediately on first ROI error instead of continuing. Default FALSE.}

\item{k}{Optional integer; number of cross-validation folds. Default NULL (use model default).}

\item{...}{Additional arguments passed on to \code{do_standard} or \code{do_randomized}.}
}
\value{
The result object from \code{do_standard} or \code{do_randomized} (often a \code{searchlight_result} or similar).
}
\description{
This function implements the generic logic for running a searchlight:
\enumerate{
  \item Checks \code{radius} and \code{method}.
  \item For "standard" searchlight, calls \code{do_standard(...)}.
  \item For "randomized", calls \code{do_randomized(...)} with \code{niter} times.
  \item Handles the \code{combiner} function or string ("pool", "average").
}
}
\details{
It does not assume any specific model type, but expects that \code{model_spec}
is compatible with \code{do_standard(...)} or \code{do_randomized(...)} in your code.
}
\examples{
\dontrun{
  # Internal base function - users should call run_searchlight instead
  # result <- run_searchlight_base(model_spec, radius=8, method="standard")
}
}
\keyword{internal}
