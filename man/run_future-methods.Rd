% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allgeneric.R, R/mvpa_iterate.R
\name{run_future}
\alias{run_future}
\alias{run_future.default}
\title{Run Future}
\usage{
run_future(obj, frame, processor, ...)

\method{run_future}{default}(
  obj,
  frame,
  processor = NULL,
  verbose = FALSE,
  analysis_type = "searchlight",
  drop_probs = FALSE,
  fail_fast = FALSE,
  ...
)
}
\arguments{
\item{obj}{An object specifying the computation.}

\item{frame}{A data frame or environment containing data for the computation.}

\item{processor}{A function or object specifying how to process the frame.}

\item{...}{Additional arguments passed to the method-specific function.}

\item{verbose}{Logical; print progress messages if \code{TRUE}.}

\item{analysis_type}{The type of analysis (e.g., "searchlight").}
}
\value{
A tibble containing the processed results.
}
\description{
Run a future-based computation defined by the object and frame.
}
\details{
If the \pkg{progressr} package is installed, this method emits per-task
progress updates from parallel workers. Progress handling is enabled for
the scope of this call and uses the currently configured handlers.
}
\examples{
frame <- tibble::tibble(
  .id = 1:2,
  rnum = c("roi1", "roi2"),
  roi = list(1:3, 4:5),
  size = c(3, 2)
)
mod_spec <- list(process_roi = function(mod_spec, roi, rnum, ...) {
  tibble::tibble(
    result = list(mean(roi)),
    indices = list(seq_along(roi)),
    performance = list(NULL),
    id = rnum
  )
})
run_future(mod_spec, frame, NULL)

}
\keyword{internal}
