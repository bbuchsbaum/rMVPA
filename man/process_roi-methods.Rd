% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allgeneric.R, R/custom.R
\name{process_roi}
\alias{process_roi}
\alias{process_roi.default}
\alias{process_roi.custom_internal_model_spec}
\title{Process ROI}
\usage{
process_roi(mod_spec, roi, rnum, ...)

\method{process_roi}{default}(mod_spec, roi, rnum, center_global_id = NA, ...)

\method{process_roi}{custom_internal_model_spec}(mod_spec, roi, rnum, ...)
}
\arguments{
\item{mod_spec}{The model specification object.}

\item{roi}{The region of interest data.}

\item{rnum}{A numeric or string identifier for the ROI.}

\item{...}{Additional arguments passed to the method-specific function.}

\item{center_global_id}{Optional global ID of the center voxel. Defaults to NA.}
}
\value{
A tibble row containing the performance metrics for the ROI.
}
\description{
Process a region of interest (ROI) and return the formatted results.
}
\examples{
\donttest{
  ds <- gen_sample_dataset(c(4, 4, 4), 20, blocks = 2)
  cv <- blocked_cross_validation(ds$design$block_var)
  mdl <- load_model("sda_notune")
  spec <- mvpa_model(
    model = mdl,
    dataset = ds$dataset,
    design = ds$design,
    model_type = "classification",
    crossval = cv
  )
  vox <- sample(which(ds$dataset$mask > 0), 30)
  samp <- data_sample(ds$dataset, vox)
  roi_obj <- as_roi(samp, ds$dataset)
  process_roi(spec, roi_obj, 1)
}

}
