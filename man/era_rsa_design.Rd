% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/era_rsa_design.R
\name{era_rsa_design}
\alias{era_rsa_design}
\title{Build item-level ERA-RSA confounds and summaries from mvpa_design}
\usage{
era_rsa_design(
  design,
  key_var,
  phase_var,
  encoding_level = NULL,
  retrieval_level = NULL,
  block_var = NULL,
  time_var = NULL,
  phase_scoped_runs = FALSE
)
}
\arguments{
\item{design}{mvpa_design (same object passed to era_rsa_model).}

\item{key_var}{Column name or formula giving the item ID (e.g., ~ ImageID).}

\item{phase_var}{Column name or formula indicating phase (e.g., ~ Phase).}

\item{encoding_level}{Encoding phase label; default = first level of phase_var.}

\item{retrieval_level}{Retrieval phase label; default = second level of phase_var.}

\item{block_var}{Optional column giving block/run membership.}

\item{time_var}{Optional column giving trial index or onset time.}

\item{phase_scoped_runs}{Logical; when \code{TRUE}, prefix per-item run
labels with \code{enc_} / \code{ret_} so encoding and retrieval scans
with overlapping numeric labels (e.g. both phases have runs 1, 2, 3)
are not treated as the same run during cross-phase ERA comparisons.
Default \code{FALSE} for back-compatibility; set to \code{TRUE} when
the encoding and retrieval phases come from different scan sessions.}
}
\value{
A list with elements:
  \itemize{
    \item items: factor of item IDs used (intersection of E/R)
    \item item_block: optional factor of per-item block labels
    \item item_time_enc, item_time_ret: optional numeric vectors
    \item item_lag: optional numeric vector (ret - enc)
    \item item_run_enc, item_run_ret: optional per-item run factors
    \item confound_rdms: named list of item-level RDMs (block/time/run)
  }
}
\description{
Constructs per-item summaries (block/run/time/lag) and item-level confound RDMs
that plug directly into \code{era_rsa_model(..., confound_rdms=...)}. Reuses
temporal helpers in R/temporal_rdms.R to avoid redundancy.
}
\examples{
\dontrun{
  # See era_rsa_model for full ERA-RSA workflow with design construction
}
}
