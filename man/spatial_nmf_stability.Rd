% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_nmf_inference.R
\name{spatial_nmf_stability}
\alias{spatial_nmf_stability}
\title{Bootstrap Stability for Spatial NMF Components}
\usage{
spatial_nmf_stability(
  x = NULL,
  X = NULL,
  fit = NULL,
  graph = NULL,
  lambda = NULL,
  n_boot = 200,
  sample = c("bootstrap", "subsample"),
  sample_frac = 1,
  init = c("nndsvd", "random"),
  normalize = c("H", "none"),
  similarity = c("cosine", "cor"),
  match = c("greedy"),
  top_frac = 0.1,
  seed = NULL,
  return_maps = FALSE,
  ...
)
}
\arguments{
\item{x}{A spatial_nmf_maps_result or spatial_nmf_fit object.}

\item{X}{Optional data matrix (n x p) if not included in x.}

\item{fit}{Optional spatial_nmf_fit object (if x is not provided).}

\item{graph}{Optional graph Laplacian list (required if lambda > 0).}

\item{lambda}{Spatial regularization strength (defaults to fit$lambda).}

\item{n_boot}{Number of bootstrap samples.}

\item{sample}{One of "bootstrap" or "subsample".}

\item{sample_frac}{Fraction of subjects to sample.}

\item{init}{NMF initialization for bootstrap fits.}

\item{normalize}{Component normalization ("H" rescales rows to sum 1).}

\item{similarity}{Similarity measure for component matching ("cosine" or "cor").}

\item{match}{Matching strategy (currently "greedy").}

\item{top_frac}{Fraction of top voxels used to compute selection frequency.}

\item{seed}{Optional RNG seed.}

\item{return_maps}{Logical; return stability maps as NeuroVol/NeuroSurface.}

\item{...}{Additional arguments passed to spatial_nmf_fit.}
}
\value{
A list with stability summaries and optional maps.
}
\description{
Estimates voxelwise stability of component maps via bootstrap resampling.
}
