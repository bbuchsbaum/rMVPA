% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_output.R
\name{build_maps_from_schema}
\alias{build_maps_from_schema}
\title{Build spatial output maps from an output schema}
\usage{
build_maps_from_schema(schema, perf_mat, dataset, ids)
}
\arguments{
\item{schema}{Named list; names are metric labels, values are
\code{"scalar"} or \code{"vector[N]"}.}

\item{perf_mat}{Numeric matrix with rows = ROIs and columns = metrics.}

\item{dataset}{The dataset object (used by \code{\link{build_output_map}}).}

\item{ids}{Integer vector of ROI center IDs corresponding to rows of
\code{perf_mat}.}
}
\value{
A named list of spatial map objects (one per schema entry for
  scalar, N per entry for vector types).
}
\description{
Uses a named schema list to select columns from a performance matrix
and create one spatial map per metric.  Each schema entry is either
\code{"scalar"} (one column) or \code{"vector[N]"} (N consecutive columns).
}
\keyword{internal}
