% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pattern_core.R
\name{pattern_control}
\alias{pattern_control}
\title{Control parameters for the pattern model estimator}
\usage{
pattern_control(
  max_rank = 8L,
  x_scale = c("none", "sd"),
  y_scale = c("none", "sd"),
  noise = list(type = "diag_lowrank", rank = "auto", max_rank = 10L, shrink = 0.1),
  lambda_2 = 0,
  max_outer = 50L,
  tol = 1e-08,
  refine_path = FALSE
)
}
\arguments{
\item{max_rank}{Maximum rank considered (capped at the eligible rank:
number of classes minus one for categorical targets, the effective
number of target dimensions otherwise, and never above the number of
features or observations).}

\item{x_scale}{Feature scaling on training rows: \code{"none"} (centre
only) or \code{"sd"}.}

\item{y_scale}{Continuous-target scaling before whitening: \code{"none"}
or \code{"sd"}.}

\item{noise}{Residual covariance specification: a list with \code{type}
(\code{"diag_lowrank"}, \code{"diag"}, or \code{"identity"}),
\code{rank} (\code{"auto"} selects components above the
Marchenko-Pastur edge, or an integer), \code{max_rank}, and
\code{shrink} (shrinkage of the diagonal toward its median).}

\item{lambda_2}{Reserved for the penalized solver; must be 0. A ridge on
the patterns turns the A-step into a Sylvester equation under a general
residual covariance, so it is rejected rather than solved approximately.}

\item{max_outer}{Maximum number of alternating (C-step, A-step) updates.}

\item{tol}{Relative objective change that declares convergence.}

\item{refine_path}{Logical; when fitting a rank path, run the alternating
refinement for every rank (default \code{FALSE}: path solutions are the
exact reduced-rank optima, which coincide with the refined solution for
the unpenalized objective).}
}
\value{
A list of class \code{pattern_control}.
}
\description{
Control parameters for the pattern model estimator
}
\examples{
pattern_control(max_rank = 3)
}
