% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/banded_ridge_da_model.R
\name{banded_ridge_da}
\alias{banded_ridge_da}
\alias{grouped_ridge_da}
\title{Convenience wrapper: build a grouped-ridge domain-adaptation model from matrices}
\usage{
banded_ridge_da(
  dataset,
  X_train,
  spec = NULL,
  X_test = NULL,
  gamma = NULL,
  drop_null = TRUE,
  renormalize = FALSE,
  block_var_test = NULL,
  ...
)

grouped_ridge_da(
  dataset,
  X_train,
  spec = NULL,
  X_test = NULL,
  gamma = NULL,
  drop_null = TRUE,
  renormalize = FALSE,
  block_var_test = NULL,
  ...
)
}
\arguments{
\item{dataset}{mvpa_dataset with train_data/test_data.}

\item{X_train}{Train predictor matrix (T_train × D) or a `feature_sets` object.}

\item{spec}{Feature-set spec for matrix inputs, created by `blocks()` or `by_set()`.
Ignored if `X_train` is already a `feature_sets`.}

\item{X_test}{Optional test predictor matrix (T_test × D) or a `feature_sets` object.}

\item{gamma}{Optional alignment matrix used when `X_test` is NULL. See `expected_features()`.}

\item{drop_null, renormalize}{Passed to `expected_features()` when using `gamma`.}

\item{block_var_test}{Optional test run/block vector (length T_test).}

\item{...}{Passed through to `banded_ridge_da_model()`.}
}
\value{
A model spec of class `banded_ridge_da_model`.

A model spec of class `banded_ridge_da_model`.
}
\description{
`banded_ridge_da()` is a convenience wrapper that builds:
\itemize{
  \item a `feature_sets` object for train predictors,
  \item a test `feature_sets` object (from `X_test` or from `gamma` via `expected_features()`),
  \item a `feature_sets_design`, and
  \item the final `banded_ridge_da_model` spec.
}

Preferred name for `banded_ridge_da()`. See that function for full details.
}
\details{
Use this when you already have \code{X_train} (TR×features) as a single matrix
and you want to declare sets via `blocks()` or `by_set()`.
}
\examples{
\dontrun{
ms <- grouped_ridge_da(
  dataset = dset,
  X_train = X_enc,
  spec = blocks(low = 100, mid = 100, high = 100, sem = 100),
  gamma = gamma,
  block_var_test = recall_runs,
  mode = "stacked",
  lambdas = c(low = 10, mid = 10, high = 10, sem = 10),
  alpha_recall = 0.2
)
}
}
\seealso{
\code{\link{banded_ridge_da_model}}, \code{\link{grouped_ridge_da_model}},
  \code{\link{blocks}}, \code{\link{by_set}}
}
