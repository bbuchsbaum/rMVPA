% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repmed_model.R
\name{repmed_model}
\alias{repmed_model}
\title{Representational mediation model (ReNA-RM)}
\usage{
repmed_model(
  dataset,
  design,
  repmed_des,
  key_var,
  distfun = cordist(method = "pearson"),
  ...
)
}
\arguments{
\item{dataset}{An \code{mvpa_dataset}.}

\item{design}{An \code{mvpa_design}.}

\item{repmed_des}{Output of \code{\link{repmed_design}}.}

\item{key_var}{Column or formula giving item identity in the mvpa_design
(e.g. \code{~ ImageID}).}

\item{distfun}{Distance function to compute the ROI RDM (e.g. \code{cordist(method = "pearson")} ).}

\item{...}{Extra fields stored on the model spec.}
}
\value{
A model spec of class \code{"repmed_model"}.
}
\description{
For each ROI/searchlight, tests whether the ROI RDM mediates the relationship
between a predictor RDM (X) and an outcome RDM (Y), optionally including
confound RDMs.
}
\examples{
\dontrun{
  # Requires repmed_design with X_rdm and Y_rdm
  # model <- repmed_model(dataset, design, repmed_des, key_var=~ImageID)
}
}
