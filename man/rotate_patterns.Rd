% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pattern_maps.R
\name{rotate_patterns}
\alias{rotate_patterns}
\alias{predict.pattern_view}
\title{Display patterns in orthogonal spatial and target coordinates}
\usage{
rotate_patterns(fit, spatial = "varimax", target = "varimax")

\method{predict}{pattern_view}(object, ...)
}
\arguments{
\item{fit}{A fitted pattern model or global result with a refit.}

\item{spatial, target}{Orthogonal matrices, \code{"varimax"}, or \code{"none"}.}

\item{object}{A pattern view.}

\item{...}{Arguments passed to \code{predict.pattern_fit}.}
}
\value{
A \code{pattern_view} containing \code{L_b = A Q_b},
  \eqn{H = Q_b^T Q_t}, and \code{L_t = C Q_t}, in the fitted feature and
  whitened target coordinates. Thus \eqn{L_b H L_t^T} reconstructs
  \eqn{A C^T}. Back-transforms, a basis identifier, and the Frobenius
  reconstruction error (evaluated without a full feature by target matrix)
  are retained.
}
\description{
Display patterns in orthogonal spatial and target coordinates
}
\details{
The underlying fit is unchanged. Predictions (including calibrated
  scores) and invariant maps delegate to that fit and are bit-identical.
  Display columns are not separately identified scientific components.
}
