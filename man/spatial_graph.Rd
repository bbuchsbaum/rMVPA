% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allgeneric.R, R/pattern_spatial.R
\name{spatial_graph}
\alias{spatial_graph}
\alias{spatial_graph.mvpa_image_dataset}
\alias{spatial_graph.mvpa_multibasis_image_dataset}
\alias{spatial_graph.mvpa_surface_dataset}
\alias{spatial_graph.mvpa_clustered_dataset}
\alias{spatial_graph.default}
\title{Spatial Adjacency Graph Aligned to Dataset Features}
\usage{
spatial_graph(x, ...)

\method{spatial_graph}{mvpa_image_dataset}(x, neighbors = 6, ...)

\method{spatial_graph}{mvpa_multibasis_image_dataset}(x, neighbors = 6, connect_basis = FALSE, ...)

\method{spatial_graph}{mvpa_surface_dataset}(x, ...)

\method{spatial_graph}{mvpa_clustered_dataset}(x, neighbors = 6, ...)

\method{spatial_graph}{default}(
  x,
  feature_ids = NULL,
  weighted = FALSE,
  domain_type = "custom",
  ...
)
}
\arguments{
\item{x}{A dataset (\code{\link{mvpa_dataset}} and friends) or a raw
adjacency specification.}

\item{...}{Additional arguments passed to methods; see
\code{\link{spatial_graph.mvpa_image_dataset}}.}

\item{neighbors}{Voxel neighbourhood for volumetric grids: 6 (faces),
18 (faces and edges), or 26 (faces, edges, and corners).}

\item{connect_basis}{Logical; for multibasis datasets, also connect each
voxel to the same voxel in every other basis channel (default
\code{FALSE}: channels form disconnected components, so smoothing never
crosses channels).}

\item{feature_ids}{Integer identifiers mapping graph vertices to dataset
locations when a raw adjacency is supplied. Defaults to \code{seq_len(n)}.}

\item{weighted}{Logical; keep edge weights of a raw adjacency instead of
binarizing them.}

\item{domain_type}{Label for a raw adjacency's domain (default
\code{"custom"}).}
}
\value{
A \code{spatial_graph} object.
}
\description{
Build a sparse adjacency graph whose vertex \eqn{j} refers to column
\eqn{j} of the feature matrix returned by \code{\link{get_feature_matrix}}
for the same dataset. This alignment is the contract every spatially
regularized estimator in rMVPA relies on.
}
\details{
Methods exist for volumetric datasets (grid adjacency over the mask;
6, 18, or 26 neighbours), multibasis volumetric datasets (one grid graph
per basis channel, disconnected across channels unless
\code{connect_basis = TRUE}), surface datasets (mesh adjacency from
\pkg{neurosurf}, restricted to masked nodes), clustered datasets
(two parcels are adjacent when any of their voxels are), and raw
adjacency matrices supplied directly (\code{matrix}, \code{Matrix}, or a
list with an \code{A} element).

The result is a list of class \code{spatial_graph} with the sparse
symmetric adjacency \code{A}, its \code{degree} vector, the combinatorial
Laplacian \code{L = diag(degree) - A}, the \code{feature_ids} that map
graph vertices back to dataset locations, the number of features
\code{n_features}, a \code{domain_type}, a \code{geometry_id} string
identifying the geometry the graph was built from, and, for multibasis
data, a \code{basis} vector giving each column's basis channel. The object
also carries the \code{A}, \code{weighted}, and \code{degree} fields that
the graph-regularized NMF functions accept as their \code{graph} argument.

For clustered datasets the graph vertices are the parcels, in the column
order of the cluster time series, and \code{feature_ids} holds the actual
cluster labels (which need not be \code{1..K}). Column positions, not
labels, index the feature matrix.
}
\examples{
ds <- gen_sample_dataset(c(5, 5, 5), 10)
g <- spatial_graph(ds$dataset)
g$n_features == ncol(get_feature_matrix(ds$dataset))
}
\seealso{
\code{\link{restrict_graph}}, \code{\link{graph_edges}}
}
