Archived code that has been excised from the active package lives here.

The `archive/` tree is excluded from package builds via `.Rbuildignore` so the
code remains available for migration, comparison, and possible extraction into a
separate package without staying on the live `rMVPA` API surface.
